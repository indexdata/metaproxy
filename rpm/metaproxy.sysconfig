# Defaults for metaproxy
OPTIONS="-u ${SERVER_USER} -l /var/log/metaproxy.log -c /etc/metaproxy/metaproxy.xml"
# The following options should not be set, because init.d script 
# already sets them: -p, -D, -t
